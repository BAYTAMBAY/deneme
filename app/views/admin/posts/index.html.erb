<% content_for :title, "Admin Posts | OneTicketAway" %>

<section class="hero-card admin-hero">
  <div>
    <p class="eyebrow">Admin Panel</p>
    <h1 class="page-title">Blog yazilari</h1>
    <p>Yayin akisini buradan yonetebilirsin. Taslak yazilarini sakla, kapak gorseli ekle ve hazir oldugunda tek tikla yayina al.</p>
  </div>

  <div class="admin-hero__stats">
    <div class="stat-chip">
      <strong><%= @posts.count %></strong>
      <span>Toplam yazi</span>
    </div>
    <div class="stat-chip">
      <strong><%= @posts.count(&:published?) %></strong>
      <span>Yayinda</span>
    </div>
    <div class="actions">
      <%= link_to "Yeni yazi", new_admin_post_path, class: "button" %>
    </div>
  </div>
</section>

<section class="admin-table">
  <table>
    <thead>
      <tr>
        <th>Baslik</th>
        <th>Durum</th>
        <th>Yayin tarihi</th>
        <th>Islemler</th>
      </tr>
    </thead>
    <tbody>
      <% if @posts.any? %>
        <% @posts.each do |post| %>
          <tr>
            <td>
              <strong><%= post.title %></strong><br>
              <span class="article-meta">/<%= post.slug %></span>
            </td>
            <td><span class="tag <%= post.published? ? "tag--success" : "" %>"><%= post.status %></span></td>
            <td><%= post.published_at ? l(post.published_at, format: :long) : "-" %></td>
            <td>
              <div class="actions actions--compact">
                <%= link_to "Gor", admin_post_path(post), class: "button button--secondary" %>
                <%= link_to "Duzenle", edit_admin_post_path(post), class: "button button--secondary" %>
                <% if post.published? %>
                  <%= link_to "Canli sayfa", post_path(post), class: "button button--secondary" %>
                <% end %>
                <%= button_to "Sil", admin_post_path(post), method: :delete, class: "button button--danger", form: { data: { turbo_confirm: "Bu yazi silinsin mi?" } } %>
              </div>
            </td>
          </tr>
        <% end %>
      <% else %>
        <tr>
          <td colspan="4">Henuz hic yazi olusturulmamis.</td>
        </tr>
      <% end %>
    </tbody>
  </table>
</section>
